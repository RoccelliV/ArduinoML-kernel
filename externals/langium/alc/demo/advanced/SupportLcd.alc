app SupportLcd initial state off {
	bricks
		sensor button : 10
		actuator ["led"] led: 9
		screen lcd : 8
	
	states
		off {
			led <= LOW
			lcd <= led
			button is HIGH => on
		}
		on {
			led <= HIGH
			lcd <= led
			button is HIGH => off
		}
}